{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "implements(Microsoft.IDialog)",
  "title": "GetProfileWithBookingInfoAction",
  "description": "This will return a list of employees with booking info",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "employees": {
      "$ref": "schema:#/definitions/arrayExpression",
      "title": "Employees",
      "description": "Raw employees"
    },
    "result": {
      "$ref": "schema:#/definitions/stringExpression",
      "title": "Result",
      "description": "The property that transformed employees are assign to"
    }
  }
}