[Icons](icons.en-us.lg)

# MoreBookingInfoButton()
-```
{
    "type": "ActionSet",
    "actions": [
        ${MoreBookingInfoAction()}
    ]
}
```

# MoreBookingInfoAction()
-```
{
    "type": "Action.OpenUrl",
    "title": "📈 More Booking Info",
    "url": "${settings.App.CRMBookingEndpoint}"
}
```

# ViewCalendarAction(displayName, bookedDays, sourceIntent)
- ```
{
    "type": "Action.Submit",
    "title": "View Calendar",
    "data": 
    {
        "intent": "GetCalendar",
        "contact": ${dialog.nameEntity},
        ${IntentRedirectTelemetry('ViewCalendar', sourceIntent, 'View Calendar')},
        "msteams": {
            "type": "messageBack",
			"displayText": "Show calendar for ${displayName} ${BookedDaysText(bookedDays)}"
        }
    },
    "iconUrl": "${OutlookCalendarIcon()}"
}
```

# ViewProfileCardAction(firstName, lastName)
- ```
{
    "type": "Action.Submit",
    "title": "View Profile Card",
    "data": 
    {
        "intent": "GetProfile",
        "contact": {
            "firstName": [
                "${firstName}"
            ],
            "lastName": [
                "${lastName}"
            ]
        },
        "msteams": {
            "type": "messageBack",
            "displayText": "${firstName} ${lastName}"
        }
    }
}
```

# BookedDaysText(bookedDays)
- IF: ${bookedDays > 0}
    - (${bookedDays} days booked 🗓)
- ELSE:
    - 

# IntentRedirectTelemetry(actionType, sourceIntent, action)
- ```
"redirect": {
    "actionType": "${actionType}",
    "sourceIntent": "${sourceIntent}",
    "action": "${action}"
}
```
